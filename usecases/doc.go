/*
Package usecases implements the usecase layer functionality in this
clean-architecture system. The usecase layer has dependency on the
entity layer and nothing else: all other concerns such as DB and
networking are injected at runtime.
*/
package usecases
